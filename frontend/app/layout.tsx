import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'StageTree',
  description: 'At StageTree, we are dedicated to creating the best digital products for your business. Whether you need online systems, apps, or websites, our team of experts will deliver exceptional solutions that drive success.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
